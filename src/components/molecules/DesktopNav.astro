---
import { navData } from "@/assets/data";
---

<nav class="fixed top-3 z-50 hidden w-full justify-center lg:flex">
  <ul class="flex gap-x-10 rounded-full p-6">
    {
      navData.map((link) => (
        <li>
          <a href={link.href} class="text-xl font-medium">
            {link.name}
          </a>
        </li>
      ))
    }
  </ul>
</nav>
